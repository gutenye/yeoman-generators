{
  "name": "<%=project%>",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*/src/**/*.{js,jsx,json,css}": [
      "prettier --no-semi --single-quote --trailing-comma=es5 --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^0.0.0",
    "lint-staged": "^0.0.0",
    "prettier": "^0.0.0"
    "bcrypt": "^1.0.2",
    "gudatagen": "1.0.5",
    "babel-preset-env": "^1.6.1"
  }
}
